# Arcane Service Configuration
# Global variables (PUID, PGID, TZ, DOMAIN) are in ../.env.global

APP_URL=https://arcane.homelab.home.arpa
PUBLIC_URL=https://arcane.homelab.home.arpa
TRUST_PROXY=true

# Generate strong random values before using in production
ENCRYPTION_KEY=replace-me-with-strong-random-value
JWT_SECRET=replace-me-with-strong-random-value
